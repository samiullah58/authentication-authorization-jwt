#### Add remote repo origin ###
git remote add origin https://github.com/samiullah58/authentication-authorization-jwt.git

### Specifying branch on which you want to push the repo ###
git branch -m main

### Commiting first commit ###
git commit -m "message for the commit"

### Pushing the whole repo to that branch ###
git push -u origin main